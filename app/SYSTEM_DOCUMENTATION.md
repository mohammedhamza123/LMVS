# توثيق نظام إدارة رخص السيارات والمخالفات

## 📋 نظرة عامة على النظام

نظام شامل لإدارة رخص السيارات والمخالفات المرورية يتكون من:
- **Backend API** (FastAPI) - الخادم الرئيسي
- **تطبيق المواطن** (Flutter) - للمواطنين
- **تطبيق الإدارة** (Flutter) - للموظفين الإداريين

---

## 👥 الأدوار والصلاحيات

### 1. المواطن (CITIZEN)
**الوصف:** المستخدم الأساسي للنظام - المواطن الذي يطلب رخصة قيادة أو يدفع مخالفة.

**الصلاحيات:**
- تسجيل حساب جديد
- تسجيل الدخول
- تقديم طلب رخصة قيادة جديدة
- متابعة حالة طلب الرخصة
- عرض امتحاناته
- عرض مخالفاته
- دفع المخالفات
- طلب تجديد الرخصة
- طلب استبدال الرخصة
- تحديث FCM token للإشعارات

---

### 2. مسؤول الرخص (LICENSE_OFFICER)
**الوصف:** الموظف المسؤول عن إدارة طلبات الرخص والامتحانات.

**الصلاحيات:**
- مراجعة طلبات الرخص المعلقة
- الموافقة أو رفض طلبات الرخص
- جدولة امتحانات للمواطنين (3 امتحانات: نظري، عملي، طبي)
- تسجيل نتائج الامتحانات
- إصدار الرخص بعد اجتياز جميع الامتحانات
- ترحيل الرخص لاعتماد رئيس القسم
- طباعة الرخص
- إدارة طلبات تجديد الرخص
- إدارة طلبات استبدال الرخص
- عرض جميع الرخص
- دفع المخالفات (للمواطنين الذين يأتون لدفعها)

---

### 3. مسؤول المخالفات (VIOLATION_OFFICER)
**الوصف:** الموظف المسؤول عن إدارة المخالفات المرورية.

**الصلاحيات:**
- إضافة مخالفة جديدة (بالرقم الوطني أو رقم الرخصة)
- البحث عن مخالفات مواطن بالرقم الوطني
- دفع المخالفات
- إصدار إيصال دفع
- إلغاء مخالفة (مع تسجيل السبب)
- تعديل مخالفة (قبل الدفع فقط)
- إدارة أنواع المخالفات (إضافة، تعديل، حذف)
- عرض إحصائيات المخالفات (يومية، أسبوعية، شهرية، سنوية)
- عرض التقارير المالية

---

### 4. المدير العام (SUPER_ADMIN)
**الوصف:** المدير المسؤول عن إدارة النظام بالكامل.

**الصلاحيات:**
- جميع صلاحيات الأدوار الأخرى
- إدارة المستخدمين (إنشاء، تعديل، حذف، تعطيل)
- إيقاف مستخدم مؤقتاً
- إلغاء إيقاف مستخدم
- إدارة أنواع الامتحانات
- إدارة أنواع المخالفات
- إدارة أنواع الرخص
- إدارة فئات الرخص (A/B)
- عرض جميع الرخص
- اعتماد/توقيع الرخص المرحلة من مسؤول الرخص
- عرض إحصائيات النظام الشاملة

---

## 🔄 سيناريوهات العمل التفصيلية

### سيناريو 1: طلب رخصة قيادة جديدة

#### من تطبيق المواطن:
1. **تسجيل الدخول/التسجيل**
   - المواطن يسجل حساب جديد أو يسجل دخول
   - يتم حفظ FCM token للإشعارات

2. **تقديم طلب رخصة**
   - المواطن يملأ بيانات الرخصة:
     - نوع الرخصة (من القائمة المعرفة من قبل المدير)
     - الفئة (A أو B)
     - البيانات الشخصية (الاسم، تاريخ الميلاد، الجنس، فصيلة الدم، إلخ)
     - رفع المستندات (صورة شخصية، شهادة الإقامة، شهادة الميلاد، جواز السفر)
   - يتم إنشاء طلب برخصة بحالة `PENDING`

3. **متابعة الطلب**
   - المواطن يرى حالة طلبه في التطبيق
   - الحالات: `PENDING` → `APPROVED` → `EXAM_PASSED` → `ISSUED`

#### من تطبيق الإدارة (مسؤول الرخص):
1. **مراجعة الطلبات**
   - مسؤول الرخص يرى قائمة الطلبات المعلقة
   - يختار طلب للمراجعة

2. **الموافقة أو الرفض**
   - إذا وافق: الحالة تصبح `APPROVED`
   - إذا رفض: الحالة تصبح `REJECTED` (ينتهي العمل)
   - يتم إرسال إشعار للمواطن

3. **جدولة الامتحانات**
   - بعد الموافقة، مسؤول الرخص يحدد مواعيد 3 امتحانات:
     - امتحان نظري
     - امتحان عملي
     - امتحان طبي
   - يتم إنشاء 3 سجلات امتحان للمواطن

4. **تسجيل نتائج الامتحانات**
   - مسؤول الرخص يسجل نتيجة كل امتحان (نجح/فشل)
   - إذا نجح في جميع الامتحانات: الحالة تصبح `EXAM_PASSED`
   - إذا فشل في أي امتحان: الحالة تصبح `EXAM_FAILED`

5. **إصدار الرخصة**
   - بعد اجتياز جميع الامتحانات، مسؤول الرخص يصدر الرخصة
   - يتم إنشاء رقم رخصة فريد وباركود
   - الحالة تصبح `ISSUED`
   - يتم إرسال إشعار للمواطن

6. **الترحيل للاعتماد (اختياري)**
   - مسؤول الرخص يمكنه ترحيل الرخصة لاعتماد رئيس القسم
   - رئيس القسم (SUPER_ADMIN) يعتمد/يوقع الرخصة

7. **الطباعة**
   - مسؤول الرخص يطبع الرخصة من قائمة الرخص القابلة للطباعة

---

### سيناريو 2: تجديد رخصة

#### من تطبيق المواطن:
1. **طلب تجديد**
   - المواطن يختار الرخصة التي يريد تجديدها
   - يقدم طلب تجديد
   - يتم إنشاء طلب تجديد بحالة `PENDING`

#### من تطبيق الإدارة (مسؤول الرخص):
1. **مراجعة طلبات التجديد**
   - مسؤول الرخص يرى قائمة طلبات التجديد المعلقة
   - يختار طلب للمراجعة

2. **الموافقة أو الرفض**
   - إذا وافق: الحالة تصبح `APPROVED`
   - يتم تحديث تاريخ انتهاء الرخصة
   - يتم إرسال إشعار للمواطن

---

### سيناريو 3: استبدال رخصة

#### من تطبيق المواطن:
1. **طلب استبدال**
   - المواطن يختار الرخصة التي يريد استبدالها
   - يقدم طلب استبدال مع السبب
   - يتم إنشاء طلب استبدال بحالة `PENDING`

#### من تطبيق الإدارة (مسؤول الرخص):
1. **مراجعة طلبات الاستبدال**
   - مسؤول الرخص يرى قائمة طلبات الاستبدال المعلقة
   - يختار طلب للمراجعة

2. **الموافقة أو الرفض**
   - إذا وافق: يتم إنشاء رخصة جديدة
   - يتم إرسال إشعار للمواطن

---

### سيناريو 4: إضافة مخالفة ودفعها

#### من تطبيق الإدارة (مسؤول المخالفات):
1. **إضافة مخالفة**
   - مسؤول المخالفات يبحث عن مواطن بالرقم الوطني
   - يختار نوع المخالفة من القائمة
   - يدخل تفاصيل المخالفة (الوصف، الموقع، التاريخ)
   - يتم إنشاء مخالفة بحالة `PENDING`
   - يتم إرسال إشعار للمواطن بنوع المخالفة

2. **البحث عن مخالفات مواطن**
   - مسؤول المخالفات يبحث بالرقم الوطني
   - يرى جميع مخالفات المواطن

3. **دفع مخالفة**
   - عندما يأتي المواطن لدفع المخالفة
   - مسؤول المخالفات يضغط "دفع"
   - الحالة تصبح `PAID`
   - يتم إرسال إشعار للمواطن بتأكيد الدفع

4. **إصدار إيصال**
   - مسؤول المخالفات يمكنه إصدار إيصال دفع يحتوي على:
     - رقم الإيصال
     - رقم المخالفة
     - الرقم الوطني
     - المبلغ
     - تاريخ الدفع
     - اسم الموظف

5. **إلغاء مخالفة**
   - مسؤول المخالفات يمكنه إلغاء مخالفة غير مدفوعة
   - يجب إدخال سبب الإلغاء
   - الحالة تصبح `CANCELLED`

6. **تعديل مخالفة**
   - مسؤول المخالفات يمكنه تعديل مخالفة غير مدفوعة
   - يجب إدخال سبب التعديل
   - يمكن تعديل: النوع، الوصف، الموقع، المبلغ

#### من تطبيق المواطن:
1. **عرض المخالفات**
   - المواطن يرى جميع مخالفاته في التطبيق
   - يرى حالة كل مخالفة (غير مدفوعة، مدفوعة، ملغاة)

2. **دفع المخالفة**
   - المواطن يمكنه دفع المخالفة من التطبيق
   - الحالة تصبح `PAID`
   - يتم إرسال إشعار بتأكيد الدفع

---

### سيناريو 5: إدارة النظام (المدير العام)

#### من تطبيق الإدارة (SUPER_ADMIN):
1. **إدارة المستخدمين**
   - إنشاء مستخدم جديد (مواطن أو موظف)
   - تعديل بيانات مستخدم
   - حذف مستخدم
   - تعطيل/تفعيل مستخدم
   - إيقاف مستخدم مؤقتاً (مع السبب والتاريخ)

2. **إدارة أنواع الامتحانات**
   - إضافة نوع امتحان جديد
   - تعديل نوع امتحان
   - حذف نوع امتحان
   - تحديد: الاسم، الوصف، درجة النجاح، المدة، السعر

3. **إدارة أنواع المخالفات**
   - إضافة نوع مخالفة جديد
   - تعديل نوع مخالفة
   - حذف نوع مخالفة
   - تحديد: الاسم، الوصف، قيمة الغرامة

4. **إدارة أنواع الرخص**
   - إضافة نوع رخصة جديد
   - تعديل نوع رخصة
   - حذف نوع رخصة
   - إضافة فئة (A/B) لنوع رخصة

5. **اعتماد الرخص**
   - عرض الرخص المرحلة من مسؤول الرخص
   - اعتماد/توقيع الرخصة

6. **الإحصائيات**
   - عرض إحصائيات شاملة:
     - عدد المستخدمين (حسب الدور)
     - عدد الرخص (حسب الحالة)
     - عدد الامتحانات
     - عدد المخالفات
     - المبالغ المالية

---

## 📱 التطبيقان

### تطبيق المواطن (xx/)
**التقنيات:** Flutter, Bloc (State Management)

**الشاشات الرئيسية:**
- شاشة تسجيل الدخول
- شاشة التسجيل
- لوحة التحكم الرئيسية
- شاشة طلب رخصة جديدة
- شاشة متابعة الرخصة
- شاشة الامتحانات
- شاشة المخالفات
- شاشة دفع المخالفة
- شاشة تجديد الرخصة
- شاشة استبدال الرخصة
- شاشة الإعدادات

**الميزات:**
- إشعارات FCM عند:
  - تغيير حالة الرخصة
  - جدولة امتحان
  - نتيجة امتحان
  - إضافة مخالفة جديدة
  - دفع مخالفة
- تحديث FCM token تلقائياً عند تسجيل الدخول

---

### تطبيق الإدارة (admin_panel_app/)
**التقنيات:** Flutter, Bloc (State Management)

**الشاشات الرئيسية:**
- شاشة تسجيل الدخول
- لوحة تحكم مسؤول الرخص:
  - طلبات الرخص المعلقة
  - جميع الرخص
  - جدولة امتحانات
  - تسجيل نتائج الامتحانات
  - الرخص القابلة للطباعة
  - طلبات التجديد
  - طلبات الاستبدال
- لوحة تحكم مسؤول المخالفات:
  - إضافة مخالفة
  - البحث عن مخالفات
  - إدارة أنواع المخالفات
  - التقارير والإحصائيات
- لوحة تحكم المدير العام:
  - إدارة المستخدمين
  - إدارة أنواع الامتحانات
  - إدارة أنواع المخالفات
  - إدارة أنواع الرخص
  - اعتماد الرخص
  - الإحصائيات الشاملة

**الميزات:**
- واجهات RTL (من اليمين لليسار)
- إشعارات FCM للمواطنين عند:
  - تغيير حالة الرخصة
  - جدولة امتحان
  - نتيجة امتحان
  - إضافة مخالفة
  - دفع مخالفة

---

## 🔌 API Endpoints الرئيسية

### Authentication (`/api/v1/auth`)
- `POST /register` - تسجيل مستخدم جديد
- `POST /login` - تسجيل الدخول

### Users (`/api/v1/users`)
- `GET /me` - معلومات المستخدم الحالي
- `PUT /me` - تحديث بيانات المستخدم
- `POST /change-password` - تغيير كلمة المرور
- `POST /update-fcm-token` - تحديث FCM token

### Licenses (`/api/v1/licenses`)
- `POST /create` - إنشاء طلب رخصة (مواطن)
- `GET /my-licenses` - رخص المستخدم (مواطن)
- `GET /pending/list` - طلبات معلقة (مسؤول رخص)
- `POST /{id}/review` - مراجعة طلب (مسؤول رخص)
- `POST /{id}/schedule-exams` - جدولة امتحانات (مسؤول رخص)
- `POST /{id}/submit-exam-result` - تسجيل نتيجة امتحان (مسؤول رخص)
- `POST /{id}/issue` - إصدار رخصة (مسؤول رخص)
- `GET /officer/all` - جميع الرخص (مسؤول رخص)
- `GET /officer/printable` - رخص قابلة للطباعة (مسؤول رخص)

### Exams (`/api/v1/exams`)
- `POST /create` - إنشاء امتحان (مسؤول رخص)
- `GET /my-exams` - امتحانات المستخدم (مواطن)
- `GET /pending/list` - امتحانات معلقة (مسؤول رخص)
- `POST /{id}/schedule` - تحديد موعد امتحان (مسؤول رخص)
- `POST /{id}/result` - تسجيل نتيجة امتحان (مسؤول رخص)

### Violations (`/api/v1/violations`)
- `POST /create` - إنشاء مخالفة (مسؤول مخالفات)
- `GET /by-national-id/{national_id}` - البحث بالرقم الوطني (مسؤول مخالفات)
- `POST /{id}/pay` - دفع مخالفة (مسؤول مخالفات أو مواطن)
- `GET /{id}/receipt` - إيصال دفع (مسؤول مخالفات)
- `POST /{id}/cancel` - إلغاء مخالفة (مسؤول مخالفات)
- `POST /{id}/modify` - تعديل مخالفة (مسؤول مخالفات)
- `GET /statistics` - إحصائيات (مسؤول مخالفات)

### Violation Types (`/api/v1/violations/types`)
- `GET /` - قائمة أنواع المخالفات (مسؤول مخالفات)
- `POST /` - إضافة نوع (مسؤول مخالفات)
- `PUT /{id}` - تعديل نوع (مسؤول مخالفات)
- `DELETE /{id}` - حذف نوع (مسؤول مخالفات)

### License Renewals (`/api/v1/renewals`)
- `POST /request` - طلب تجديد (مواطن)
- `GET /my-renewals` - طلبات تجديد المستخدم (مواطن)
- `GET /officer/pending` - طلبات معلقة (مسؤول رخص)
- `POST /{id}/approve` - الموافقة على تجديد (مسؤول رخص)

### License Replacements (`/api/v1/replacements`)
- `POST /request` - طلب استبدال (مواطن)
- `GET /my-replacements` - طلبات استبدال المستخدم (مواطن)
- `GET /officer/pending` - طلبات معلقة (مسؤول رخص)
- `POST /{id}/approve` - الموافقة على استبدال (مسؤول رخص)

### Admin (`/api/v1/admin`)
- `GET /users` - جميع المستخدمين (مدير عام)
- `POST /users` - إنشاء مستخدم (مدير عام)
- `PUT /users/{id}` - تعديل مستخدم (مدير عام)
- `DELETE /users/{id}` - حذف مستخدم (مدير عام)
- `POST /users/{id}/suspend` - إيقاف مستخدم (مدير عام)
- `GET /statistics` - إحصائيات النظام (مدير عام)
- `GET /exam-types` - أنواع الامتحانات (مدير عام)
- `POST /exam-types` - إضافة نوع امتحان (مدير عام)
- `GET /violation-types` - أنواع المخالفات (مدير عام)
- `POST /violation-types` - إضافة نوع مخالفة (مدير عام)
- `GET /license-types` - أنواع الرخص (مدير عام)
- `POST /license-types` - إضافة نوع رخصة (مدير عام)

---

## 🔔 نظام الإشعارات (FCM)

### الإشعارات المرسلة للمواطنين:
1. **عند إضافة مخالفة جديدة**
   - العنوان: "تمت إضافة مخالفة جديدة"
   - النص: نوع المخالفة والمبلغ

2. **عند دفع مخالفة**
   - العنوان: "تم دفع المخالفة بنجاح"
   - النص: رقم المخالفة والمبلغ

3. **عند تغيير حالة الرخصة**
   - العنوان: "تحديث حالة الرخصة"
   - النص: الحالة الجديدة

4. **عند جدولة امتحان**
   - العنوان: "تم جدولة امتحان"
   - النص: نوع الامتحان والتاريخ

5. **عند تسجيل نتيجة امتحان**
   - العنوان: "نتيجة الامتحان"
   - النص: النتيجة (نجح/فشل)

---

## 📊 حالات النظام

### حالات الرخصة (LicenseStatus):
- `PENDING` - قيد المراجعة
- `APPROVED` - موافق عليها
- `EXAM_PASSED` - اجتاز الامتحانات
- `EXAM_FAILED` - فشل في الامتحان
- `REJECTED` - مرفوضة
- `ISSUED` - صادرة
- `EXPIRED` - منتهية الصلاحية
- `REVOKED` - ملغاة

### حالات المخالفة (ViolationStatus):
- `PENDING` - غير مدفوعة
- `PAID` - مدفوعة
- `CANCELLED` - ملغاة

### حالات تجديد الرخصة (LicenseRenewalStatus):
- `PENDING` - قيد المراجعة
- `APPROVED` - موافق عليها
- `REJECTED` - مرفوضة

---

## 🔐 الأمان والصلاحيات

- جميع الـ endpoints تتطلب مصادقة (JWT Token)
- الصلاحيات محمية بـ `require_role()` decorator
- المواطنون يمكنهم الوصول فقط لبياناتهم الخاصة
- الموظفون يمكنهم الوصول للبيانات حسب صلاحياتهم
- المدير العام لديه صلاحيات كاملة

---

## 📝 ملاحظات مهمة

1. **FCM Token**: يجب أن يسجل المواطن دخوله مرة واحدة على الأقل لتسجيل FCM token
2. **حالات الرخصة**: الرخصة تمر بمراحل متعددة قبل الإصدار
3. **الامتحانات**: يجب اجتياز 3 امتحانات قبل إصدار الرخصة
4. **المخالفات**: يمكن تعديل أو إلغاء المخالفة فقط قبل الدفع
5. **التقارير**: مسؤول المخالفات يمكنه عرض تقارير يومية/شهرية/سنوية

---

## 🛠️ التقنيات المستخدمة

### Backend:
- FastAPI (Python)
- SQLAlchemy (ORM)
- SQLite/PostgreSQL
- JWT Authentication
- Firebase Cloud Messaging (FCM)

### Frontend:
- Flutter (Dart)
- Bloc (State Management)
- HTTP Client
- Firebase Messaging

---

تم إنشاء هذا التوثيق بناءً على الكود الحالي في `app/` بتاريخ آخر تحديث.


